---
const { kicker, title, desc } = Astro.props;
---
<div class="mb-6">
  {kicker && <p class="text-xs uppercase tracking-widest text-white/50">{kicker}</p>}
  <h2 class="mt-2 text-2xl font-semibold tracking-tight">{title}</h2>
  {desc && <p class="mt-2 max-w-3xl text-white/70">{desc}</p>}
</div>
